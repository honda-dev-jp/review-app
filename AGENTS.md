# AGENTS.md

## 対象プロジェクト

このリポジトリは、スクラッチPHPで作成した映画レビューアプリ `review-app` です。

このファイルは、Codex CLI がセキュリティレビューやコード確認を行う際の作業ルールを定義します。

## 最重要ルール

Codex CLI は、このリポジトリ内のファイルを確認する場合、以下のルールを必ず守ってください。

### 1. 秘密情報を含む可能性があるファイルは読まない

以下のファイルは、DB接続情報や環境設定などの秘密情報を含む可能性があるため、**絶対に読まないでください**。

```text
/config/env.php
/config/database.php
```

以下のような操作も禁止します。

```text
cat config/env.php
cat config/database.php
sed -n '1,120p' config/env.php
sed -n '1,120p' config/database.php
grep -n . config/env.php
grep -n . config/database.php
```

内容確認が必要に見える場合でも、これらのファイルは開かず、ユーザーに確認事項として提示してください。

### 2. 勝手に修正しない

Codex CLI は、問題を見つけても勝手にファイルを変更しないでください。

まず、以下の形式でユーザーへ指摘してください。

```text
【指摘】
問題の概要

【影響】
想定されるリスク

【確認対象】
該当ファイル・該当箇所

【修正案】
推奨する対応方針

【注意点】
修正前に確認すべきこと
```

ユーザーが明示的に「修正して」「変更して」「実装して」と依頼した場合のみ、修正案に基づいて編集を行ってください。

### 3. セキュリティレビューでは事実と推測を分ける

セキュリティレビューでは、確認できた事実と推測を混同しないでください。

以下のように分けて報告してください。

```text
【確認できた事実】
実際にコード・設定・ログから確認できた内容

【推測】
可能性として考えられるが、現時点では断定できない内容

【判断不可】
ファイル非参照・情報不足などにより判断できない内容
```

`/config/env.php` と `/config/database.php` を読まない制約により判断できない点は、無理に推測せず「判断不可」としてください。

## レビュー時に重点確認する項目

セキュリティチェックでは、以下を優先して確認してください。

### 公開ディレクトリ・直接アクセス制御

* `config/` 配下がWebから直接アクセスできない構成になっているか
* `.htaccess` で公開禁止ディレクトリが適切に拒否されているか
* `.env`、`.git`、バックアップファイル、SQLダンプ、一時ファイルが公開されていないか
* `phpinfo.php` などの診断用ファイルが残っていないか

### 認証・認可

* ログイン必須処理が未認証で実行できないか
* 管理者機能に適切な認可チェックがあるか
* 自分以外のユーザー情報や投稿を更新・削除できないか
* 退会、削除、更新などの危険操作がGETで実行されていないか

### CSRF対策

* POST、PUT、PATCH、DELETE相当の処理にCSRF検証があるか
* 削除・退会・更新などの状態変更処理がCSRF保護されているか

### XSS対策

* ユーザー入力をHTML出力する箇所でエスケープされているか
* レビュー本文、返信本文、ユーザー名、プロフィール文、作品名などを安全に表示しているか
* HTMLを許可している箇所がある場合、その理由とサニタイズ有無を確認すること

### SQLインジェクション対策

* ユーザー入力をSQL文字列に直接連結していないか
* プリペアドステートメントや安全なDBアクセス関数を使用しているか
* 検索条件、ID指定、ログイン処理、管理画面の絞り込み条件を重点確認すること

### セッション管理

* ログイン成功時にセッションIDを再生成しているか
* ログアウト時にセッションを破棄しているか
* セッション固定攻撃への対策があるか
* Cookie属性の設定に問題がないか

### ファイルアップロード

* MIMEタイプを確認しているか
* 拡張子だけで判定していないか
* ファイルサイズ上限があるか
* アップロード先が直接実行可能な場所になっていないか
* 元ファイル名をそのまま保存していないか

### エラー表示

* 本番環境で詳細エラーが画面表示されないか
* DB接続情報、サーバーパス、SQL文などが画面に出ないか
* ログ出力に秘密情報が含まれていないか

### Git管理・秘密情報

* 秘密情報を含むファイルがGit管理対象になっていないか
* `.gitignore` に必要な除外設定があるか
* サンプルファイルに実パスワードや実DB名が入っていないか

ただし、`/config/env.php` と `/config/database.php` は読まないでください。

## 禁止事項

Codex CLI は以下を行わないでください。

```text
/config/env.php を読む
/config/database.php を読む
秘密情報を表示する
秘密情報を推測して補完する
勝手にファイルを修正する
勝手にリファクタリングする
勝手に依存関係を追加する
勝手にcomposer install/updateを実行する
勝手にnpm install/updateを実行する
勝手にDBマイグレーションやSQLを実行する
勝手に本番環境向けのコマンドを提案なしで実行する
```

## 作業前に確認すること

作業開始時は、まず以下を確認してください。

```bash
git status
git branch
```

未コミット変更がある場合は、変更内容を確認し、勝手に上書きしないでください。

## 報告形式

セキュリティレビュー結果は、以下の形式で報告してください。

```text
# セキュリティレビュー結果

## 総評

## 重要度: 高

## 重要度: 中

## 重要度: 低

## 判断不可

## 修正優先度

## 次に確認すべき項目
```

重要度は以下の基準で分類してください。

```text
高: 秘密情報漏えい、認証回避、任意コード実行、SQLインジェクション、外部公開すべきでないファイルの200応答など
中: 条件次第で悪用可能、権限不備、CSRF不足、XSS懸念、設定不備など
低: 保守性、軽微な設定改善、将来リスクなど
```

## 判断不可の扱い

以下に該当する場合は、断定せず「判断不可」としてください。

```text
/config/env.php を読まないと分からない内容
/config/database.php を読まないと分からない内容
本番サーバー上の設定を確認しないと分からない内容
アクセスログやエラーログがないと分からない内容
.htaccess の本番反映状況が確認できない内容
```

## ユーザーへの確認が必要な場合

判断に必要な情報が不足している場合は、勝手に推測せず、確認事項として質問してください。

例：

```text
この点は /config/database.php を読まない制約があるため、コード上からは判断できません。
本番側でDB接続が正常か、管理画面または表示画面で確認してください。
```

## 最後に

このプロジェクトでは、セキュリティと正常動作のバランスを重視します。

セキュリティ上の懸念を見つけた場合でも、即時修正ではなく、まず影響範囲・修正案・確認手順を提示してください。
